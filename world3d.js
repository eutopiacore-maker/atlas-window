import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.168.0/build/three.module.js';
const scene=new THREE.Scene();scene.background=new THREE.Color(0x9ab8bd);const camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,1000);camera.position.set(35,28,55);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(Math.min(devicePixelRatio||1,2));renderer.shadowMap.enabled=true;document.body.prepend(renderer.domElement);Object.assign(renderer.domElement.style,{position:'fixed',inset:'0',zIndex:'2'});['scene','fx','liveWorld'].forEach(id=>{const e=document.getElementById(id);if(e)e.style.display='none'});scene.add(new THREE.HemisphereLight(0xffffff,0x304020,2));const sun=new THREE.DirectionalLight(0xffd39a,3);sun.position.set(-40,70,20);scene.add(sun);const land=new THREE.Mesh(new THREE.PlaneGeometry(180,180),new THREE.MeshStandardMaterial({color:0x385b3d}));land.rotation.x=-Math.PI/2;scene.add(land);const water=new THREE.Mesh(new THREE.PlaneGeometry(55,180),new THREE.MeshStandardMaterial({color:0x347681,transparent:true,opacity:.8}));water.rotation.x=-Math.PI/2;water.position.x=-60;water.position.y=.05;scene.add(water);for(let i=0;i<90;i++){const x=-15+(i%15)*7,z=-40+Math.floor(i/15)*14,h=4+(i*7)%24,m=new THREE.Mesh(new THREE.BoxGeometry(4,h,5),new THREE.MeshStandardMaterial({color:0x9b9f94}));m.position.set(x,h/2,z);scene.add(m)}camera.lookAt(0,5,0);function loop(){requestAnimationFrame(loop);renderer.render(scene,camera)}loop();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});